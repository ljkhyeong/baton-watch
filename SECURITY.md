# 보안 정책

## 지원 범위

보안 수정은 `main` 브랜치의 최신 코드에 적용한다. 과거 커밋이나 별도로 배포한 복사본에는 보안 패치를 소급 제공하지 않는다.

이 저장소의 컨테이너와 배포 설정은 운영 배포를 위한 입력물이다. 저장소에 포함되어 있다는 사실만으로 특정 환경에 배포되었거나 외부에서 접근할 수 있음을 의미하지 않는다.

## 취약점 제보

취약점은 공개 이슈 대신 [GitHub 비공개 보안 권고](https://github.com/ljkhyeong/baton-watch/security/advisories/new)로 제보한다. 해당 기능을 사용할 수 없으면 공개 이슈에 재현 방법이나 공격 세부 정보를 적지 말고, 비공개 연락 방법이 필요하다는 사실만 남긴다.

제보에는 다음 정보를 포함한다.

- 영향을 받는 커밋 또는 버전
- 최소 재현 절차와 예상 결과
- 보안 영향과 공격에 필요한 조건
- 확인한 임시 완화 방법

실제 인증 정보, 내부 URL, 응답 본문, 운영 데이터는 제보에 포함하지 않는다. 필요한 경우 재현용으로 만든 가짜 값으로 바꾼다.

## 우선 확인 대상

다음 경계의 문제는 보안 취약점으로 우선 검토한다.

- 대상 점검의 SSRF, DNS 리바인딩, 리다이렉트, TLS 호스트 검증 우회
- 관리 API 또는 이벤트 전달 인증 우회
- 응답 본문, 인증 정보, 내부 대상 주소의 로그·메트릭 노출
- 의존성, 빌드 산출물, 컨테이너 이미지의 공급망 변조
- 작업 리스, 재시도, 멱등 처리의 우회로 인한 중복 실행이나 서비스 거부

## 공개 절차

유지보수자는 제보 내용을 확인한 뒤 영향 범위, 수정 방법, 공개 시점을 제보자와 조율한다. 수정과 배포에 필요한 합리적인 시간을 확보하기 전에 공격 세부 정보를 공개하지 않는다.
