# BATON WATCH 인계 문서

최종 수정일: 2026-09-05

## 화면 문구와 문서 정리

- BATON `b6b3d164`를 전용 브랜치 `codex/watch-state-recovery`에 커밋·푸시했다.
  화면 이름을 `운영 주의사항`, `기록·자료 검색`으로 바꾸고 업무·점검 안내를 짧게 고쳤다.
  `상태 새로고침`은 기존 결과 조회, `다시 점검`은 새 점검 요청으로 구분한다.
- BATON README의 데이터·권한·복구 설명을 나누고 상세 규칙은 PRD·API 계약으로 연결했다.
  WATCH README는 기능·요청 제한·운영 도구를 표로 나눴다. API·설정·코드 식별자는 유지했다.
- 타입 검사·프로덕션 빌드와 관련 브라우저 88개가 통과했다. 자료 상태 75개,
  운영 주의사항·화면 이동 7개, 결정·검색·인수인계 6개를 실행했다.
  WATCH는 문서만 변경해 애플리케이션 검사를 다시 실행하지 않았다.
- 실제 스크린리더 낭독과 [공개 HTTPS 검증](docs/runbooks/baton-resource-health-verification.md)은
  남아 있다. DNS·스테이징 설정은 이번에 재검증하지 않았다. 환경이 준비되면 해당 절차부터 재개한다.
  구현 커밋과 검증 범위는 [연동 확인 문서](docs/runbooks/baton-integration-review.md)에 기록했다.

## 2026-09-05 상태 표시와 독립 복원 후속 작업

- BATON `72eede3`를 별도 브랜치 `codex/watch-state-recovery`에 커밋·푸시했다. 이 브랜치에서 역할 자료 상태·최근 점검 시각·재점검 UI를 구현했다.
  기존 BATON 작업 폴더의 변경은 보존했다. 상태 조회는 워크스페이스와 별도 요청이며 네트워크 호출
  전후의 권한·원본 확인, 원격 리비전 대조, 5분 최신성 판정과 동시성 제한을 적용한다.
- `ops/export-baton-watch-snapshots.sql`과 `ops/reconcile-baton-snapshots.py`를 추가했다.
  기본은 대조, 파일 SHA-256을 지정한 실행은 같은 불변 리비전·본문 재전송이며 높은 원격 리비전이나
  충돌은 덮어쓰지 않는다. [복원 절차](docs/runbooks/baton-snapshot-recovery.md)를 따른다.
- BATON 전체 일반 726개·REST Docs 159개와 후속 보강 집중 검사, 프런트 빌드·생성 계약과
  브라우저 9개 검증이 통과했다. WATCH 도구 7개 회귀와 실제 MySQL V16 스키마의 최신 스냅샷
  내보내기·INACTIVE 보존·원본 행 불변, 변경 문서 링크·YAML 구문 검사가 통과했다.
- 초기 실제 MySQL 표본에서 전달 완료 시각을 빠뜨린 제약 위반은 표본을 수정해 다시 통과했다.
  운영 DB·공개 HTTPS·실제 토큰은 사용하지 않았다. WATCH Java 런타임과 스키마는 변경하지 않았다.
- 로컬 `actionlint`와 ShellCheck는 설치되어 있지 않아 실행하지 않았다. 변경한 CI는 YAML 구문만
  확인했으며 기존 공식 이미지 공급망 차단 상태와 공개 배포 조건은 이번에 해소하지 않았다.

## 현재 인계 상태

- 2026-09-05 대상 GET은 각 홉의 상태·헤더를 받은 뒤 본문을 읽거나 비우지 않고
  연결을 즉시 닫는다. 큰 본문·스트리밍 본문 자체는 장애 사유가 아니며 새 점검의
  `responseBytes`는 0이다. DNS 고정·리다이렉트·헤더·기한·동시성 방어와
  콜백 POST의 기존 본문 크기 제한은 유지한다. 기존 점검 바이트 설정은 구성
  호환성 검증만 유지하며 본문 전체 다운로드는 점검 범위가 아니다.
- 인증된 `POST /api/v1/resource-monitors/{resourceReference}/check-requests`를 추가했다.
  202로 새 일정을 접수하거나 도래 일정·유효 리스에 합류한다. 원본 리비전·상태·결과와
  리스를 보존하고 새 수동 예약은 리소스별 30초 간격으로 제한한다. 없는 모니터는
  404, 비활성은 409, 간격 초과 요청은 429와 `Retry-After`를 반환한다.
- V5는 nullable `last_check_requested_at`을 모니터에 추가한다. 재점검 접수는 상태
  이벤트를 만들지 않으며 배포 시 기존 절차대로 마이그레이션을 먼저 적용해야 한다.
  PRD·ADR·README·운영 절차·스킬과 스테이징 마이그레이션 증거의 V5 기대값을 맞췄다.
- 초기 BATON `5f8c45c` 검토에서는 아웃박스·보정·불변 인박스를 확인했고 상태 표시와
  WATCH 독립 복원 도구는 없었다. 이후 이 문서 첫 절의 후속 작업에서 두 기능을 추가했다.
  [연동 확인 문서](docs/runbooks/baton-integration-review.md)에 최종 구현과 검증 범위를 기록했다.

- `:bootstrap:capacityTest`는 운영 기본 일정·배치를 유지하고 25% 안팎의 5초 점검과
  100ms 점검을 혼합해 모니터별 두 번 완료와 31일 전 이력 표본 정리를 측정한다.
  12개는 첫 순회 28.157초·최대 완료 간격 65.497초, 30개는 72.883초·72.848초였다.
  100건 정리는 각각 4ms·8ms였다. [용량 시험](docs/runbooks/capacity-test.md)에
  실제 측정과 계산상 상한을 구분했다. 실제 외부 네트워크·다중 인스턴스·30일치
  전체 이력 시험이나 운영 지원 규모 인증이 아니다. 런타임 기본값은 변경하지 않았다.
- 전체 일반 테스트 437개와 12개·30개 용량 시험, 부트 JAR 라이선스 검사가 통과했다.
  일반 테스트에는 기존 실제 PostgreSQL 백업·복원·진단과 새 재점검 경합·고정 Clock
  시간 경계·유효 리스 보존, HTTP 인증·202·404·409·429 계약이 포함된다.
  문서 로컬 링크와 `git diff --check`, 변경한 셸의 구문 검사도 통과했다.
  로컬 ShellCheck 실행 파일이 없어 정적 검사는 실행하지 않았다.
- 첫 외부 점검 테스트에서 전체 기한과 소켓 기한이 경합하는 분류 기대값을 바로잡았다.
  첫 용량 시험은 기존 Testcontainers 테스트와 같은 이미지 호환성 선언을 빠뜨려
  시작하지 못했고, 선언 추가 후 12개·30개 시험이 통과했다.
- 이미지 빌드·공급망 검사, 실제 BATON 호출·공개 배포·외부 알림은 실행하지 않았다.
  아래 기록된 공식 이미지 공급망 차단 상태는 이번 작업에서 재검증하지 않았다.

- 2026-09-02 애플리케이션 안전 설정이 관리 서버 주소를 `127.0.0.1`로 고정하도록
  보강했다. Compose에 중복된 `MANAGEMENT_SERVER_ADDRESS`는 제거했으며 JAR 단독
  실행과 Compose 실행이 같은 루프백 정책을 사용한다. 외부 설정으로 관리 주소,
  상세 상태와 공개 엔드포인트 허용 목록을 완화할 수 없는지 함께 검증한다.
- 2026-09-02 `baton.watch.check.schedule.delay`를 최근 선점 배치의 최댓값에서
  현재 가장 오래된 선점 가능 점검의 DB 기준 지연으로 바꿨다. 점검 실행이 멈춰도
  독립 유지보수 레인이 값을 갱신하며 활성 리스와 아직 도래하지 않은 일정은 제외한다.
- 점검 실행 서비스의 수동 최댓값 계산과 `DueCheckBatchResult`의 지연 필드를
  제거했다. 새 집계 테이블이나 전체 적체 건수 조회 없이 기존
  `watch_monitor(next_check_at, resource_reference)` 부분 인덱스에서 첫 행만 읽는다.
  기존 메트릭 이름과 120초 경보는 유지하며 대시보드·경보·런북을 같은 의미로 맞췄다.
  런타임 의존성, DB 스키마, 메트릭 레이블, 외부 서비스와 유료 기능은 추가하지 않았다.
- 실제 Spring Boot HTTP·인증·HikariCP·PostgreSQL·점검 및 전달 예약 작업·Prometheus
  노출을 한 흐름으로 실행하는 `:bootstrap:runtimeLoadTest`를 추가했다. 기본 25개,
  최대 50개 모니터를 사용하며 일반 테스트와 분리하고 CI 결과를 7일 보관한다.
- HikariCP 연결 고갈의 비식별화된 실패와 복구, 동시 모니터 동기화, 100ms 점검,
  콜백 `503` 적체와 `204` 복구, 최종 일정 지연·이벤트 백로그 0을 확인한다. 외부
  점검기와 콜백 전송기만 Spring 테스트 더블로 바꾸며 실제 DNS·소켓·TLS·공개
  콜백·Cloudflare·외부 관측 시스템은 검증하지 않는다. 새 런타임 의존성, 스키마,
  API 계약과 유료 서비스는 추가하지 않았다. 남아 있던 영문 운영 로그도 한글화했다.

- 2026-09-01 인바운드 상태 점검은 HTTP 200과 `status: UP`뿐 아니라
  `service: baton-watch`도 함께 요구한다. 다른 서비스의 `UP` 응답이나 서비스 이름이
  없는 응답을 WATCH 정상 상태로 처리하지 않으며 Java 검증기는 추가하지 않았다.
- CI의 Trivy·라이선스 검사 로직을 `ops/scan-supply-chain.sh`로 추출했다. CI와
  스테이징 배포 절차가 같은 스크립트로 부트 JAR, 자체 이미지 세 개와 Compose에
  고정된 공식 PostgreSQL·NGINX·cloudflared 이미지 세 개를 검사한다. 자체 이미지
  세 개는 먼저 이미지 ID·OCI 리비전·아카이브
  체크섬을 보관 증거로 확정하고, 그 정확한 아카이브를 공급망 검사 입력으로 사용한다.
  한 검사에서 실패해도 나머지 취약점·라이선스 검사를 끝까지 수행하며 실제
  PostgreSQL 검증과 CodeQL은 공급망 검사보다 먼저 완료한다. 기존 보고서 디렉터리는
  덮어쓰지 않고 입력 아카이브 확인 실패는 최종 보고서 경로를 남기지 않는다.
- 실제 공급망 검사에서 마이그레이션과 WATCH 런타임의 `libexpat 2.8.3-r0`에
  `CVE-2026-66046`, `CVE-2026-76641`이 검출되어 두 이미지 모두 수정 버전
  `2.8.4-r0`으로 고정했다. 수정 커밋
  `c3b21cfd21e087421ed8a433e2e448d26a26e7ef`으로 자체 이미지 세 개를 다시 빌드하고
  보관 증거를 검증한 결과 부트 JAR와 자체 이미지 세 개의 수정 가능한
  `HIGH`·`CRITICAL` 항목은 0건이다. 공식 이미지의 아래 취약점은 그대로 차단하며
  검사 예외, 실제 배포·외부 알림 연결은 추가하지 않았다.
- 2026-09-01 ARM64에서 Compose에 고정된 공식 이미지를 실제 검사한 결과 PostgreSQL
  23건, NGINX 1건, cloudflared 12건의 수정 가능한 `HIGH`·`CRITICAL` 취약점이
  검출됐다. 공식 최신 후보인 `postgres:18.6-alpine3.23`과 `postgres:18.6-trixie`는
  각각 23건, `nginx:1.31.4-alpine-slim`은 1건,
  `cloudflare/cloudflared:2026.8.1`은 11건으로 모두 0건 기준을 통과하지 못했다.
  기준을 개선하지 않는 단순 버전 갱신과 자체 공식 이미지 재포장은 적용하지 않았다.

- 2026-08-31 인바운드 요청 실패와 수집 실패가 교차할 때 각각의 2분 대기 시간이
  초기화되어 경보가 누락되는 문제를 수정했다. 기존 주 경보는 `up == 1`과
  `probe_success == 1`이 함께 확인된 대상만 제외하고 수집 대상의 고정 레이블을 유지한다.
- 주 경보 이름·2분 기준과 수집 실패·대상 누락 보조 경보를 유지했다. 경보·메트릭·
  운영 Java·런타임 의존성은 추가하지 않았다. 주 경보는 WATCH 장애 확정이 아니라
  정상 응답 확인 실패를 뜻하며 수집 대상 자체가 사라지면 별도 5분 누락 경보가 담당한다.
- 변경은 규칙·회귀 사례·문서에 한정했다. 아래 NGINX 취약점 차단 상태와 자체 이미지
  빌드의 승인 필요 조건은 유지하며 실제 배포·알림 수신 경로는 변경하지 않았다.

- 2026-08-31 CI는 터널 Compose에서 실제 NGINX 이미지 참조를 읽어 기존 Trivy
  이미지 아카이브 검사와 `gateway.cdx.json` 생성에 포함한다. SBOM은 자체 이미지
  세 개·NGINX·부트 JAR의 다섯 개이며 체크섬·보관·`main` 출처 증명 범위에도 포함된다.
- 새 검사를 실제 실행해 기존 NGINX의 OpenSSL `libcrypto3`, `libssl3` `3.5.7-r0`에서
  `CVE-2026-14456`을 검출했다. Trivy의 HIGH 기준으로 종료 코드 1을 반환한다.
  보고서의 수정 버전은 `3.5.8-r0`이다. 공식 동일 버전·stable 이미지 다이제스트는
  기존 값과 같으며 검사 예외·보안 기준 완화는 추가하지 않았다. 이는 패키지 검사
  결과이며 현재 HTTP 전용 구성에서 실제 악용 가능성을 재현했다는 뜻은 아니다.
  수정 릴리스는 [OpenSSL 3.5.8 발표](https://openssl-library.org/news/openssl-3.5-notes/)에서도 확인했다.
- 공식 이미지 기반으로 두 패키지만 올리는 자체 게이트웨이 이미지 빌드는
  빌드·배포·보관 범위가 늘어나므로 사용자 승인 전에는 추가하지 않는다.
  현재 전체 CI와 배포 준비 완료를 주장할 수 없다.
- Blackbox Exporter의 기본 HTTP 모듈과 Prometheus 수집·선택적 경보 3개를 추가했다.
  내부 프록시·공개 HTTPS의 고정 상태 경로만 60초마다 확인한다. 요청 5초·본문
  16 KiB 상한, 리다이렉트 비추적, 공개 TLS 필수와 URL 없는 고정 레이블을 사용한다.
  WATCH 운영 Java·DB·배포 Compose·실제 수집 환경은 변경하지 않았다.
- [인바운드 점검 런북](docs/runbooks/ingress-monitoring.md)은 Exporter 비공개 접근,
  제한된 이그레스, 점검 이력·로그 비보관과 적용·원복 조건을 명시한다. 실제 설치·
  외부 계정·알림 연결·유료 서비스는 추가하지 않았다.

- 2026-08-31 스테이징 터널 오버레이에 공식 NGINX 1.30.4 Alpine slim 이미지를
  다이제스트로 고정해 추가했다. 상태 경로는 초당 10건·버스트 20건, 나머지
  `/api/v1/`은 초당 5건·버스트 10건으로 분리하며 초과 시 고정 429 문제 응답을 반환한다.
- 터널은 `watch-ingress`에만, WATCH는 기존 `watch-edge`와 `watch-db`에 연결한다.
  NGINX만 두 인바운드 네트워크를 연결한다. 원격 인그레스 서비스 주소는
  `http://watch-gateway:8080`이어야 하며 호스트·관리 포트를 공개하지 않는다.
  실제 터널 설정은 변경하지 않았다. 적용·롤백은 [요청 제한 런북](docs/runbooks/request-rate-limit.md)을 따른다.
- 로컬·스테이징 Compose는 Spring 기본 `/actuator/health/readiness`를 사용하고
  `readinessState,db`를 포함한다. liveness에는 DB를 넣지 않는다. 별도 운영 Java
  클래스나 검증기는 추가하지 않았으며 로컬 관리 주소도 컨테이너 루프백으로 고정했다.
- `ops/grafana/watch-overview.json`에 기존 지표용 12개 패널을 추가했다. 인스턴스별
  DB 적체를 합산하지 않고 수집 누락을 0으로 표시하지 않는다. 새로운 메트릭·DB 조회·
  Grafana 배포·외부 계정·알림 수신 경로는 추가하지 않았다.
- `Retry-After` 반영은 BATON 수신 계약을 확인한 후의 조건부 개선으로 남겼다.
  이벤트 전달의 기존 재시도 정책과 기본 비활성 상태는 변경하지 않았다.

- 2026-08-31 점검·전달 배치는 각 점유 전에 JDK 인터럽트 상태를 확인하도록 수정했다.
  시작 전부터 인터럽트된 경우에는 점유하지 않고, 항목 처리 중 인터럽트된 경우에는
  해당 항목의 기존 완료·재시도 처리를 시도한 뒤 다음 항목을 점유하지 않는다.
- 인터럽트 상태와 기존 리스 만료 복구를 유지한다. 운영 코드는 반복 조건 두 곳만
  바꿨으며 별도 취소 상태 객체·검증 규칙·런타임 의존성은 추가하지 않았다.
  정상 종료 대기 설정과 DNS·HTTP 취소 구현은 변경하지 않았다.
- 2026-08-31 DNS 실행기 종료 시 큐에서 제거된 조회의 호출자가 기한까지 기다리는
  문제를 수정했다. `shutdownNow()`가 반환한 대기 작업의 `Future`를 취소하고,
  `CancellationException`을 `INTERNAL_FAILURE`로 분류한다. 조회하지 않은 요청을
  대상의 DNS 장애로 집계하지 않으며 별도 작업 추적기나 런타임 의존성은 추가하지 않았다.
- 점검·전달이 공유하는 DNS 구현에 적용했다. 이미 실행 중인 JVM 리졸버의 강제 중단은
  보장하지 않으며 기존 DNS 스레드·큐 상한과 HTTP 요청 취소 처리는 유지한다.
- 2026-08-30 HTTP 시간 초과 후 소켓이 남아 후속 요청을 막는 문제를 수정했다.
  점검 GET과 이벤트 전달 POST는 작업 제출 전에 요청 객체를 만들고,
  `FutureTask` 취소 훅에서 Apache `HttpUriRequestBase.cancel()`을 호출한다.
- 시간 초과·호출자 인터럽트·HTTP 실행기 종료가 같은 요청 취소 경로를 사용한다.
  종료 시 새 작업을 거부하고 실행 중·대기 작업을 취소하며 대기 호출자도 반환한다.
  별도 감시 스레드·소켓 구현·런타임 의존성은 추가하지 않았다.
- 실제 로컬 스트리밍 서버로 연결 종료와 후속 GET·POST의 정상 처리를 확인했다.
  DNS 고정·TLS 검증·리다이렉트 정책과 기존 오류 분류·내구성 재시도는 유지한다.
- 2026-08-30 이벤트 전달의 활성화 조건 네 곳을 전달 전용 Spring `Condition`으로
  통일했다. 클라이언트·계측 래퍼·실행 서비스·예약 작업이 같은 불리언 변환을
  사용하므로 `on`·`yes`·`1`과 앞뒤 공백이 있는 값에서도 실제 설정 바인딩과 일치한다.
  기본 비활성 상태, 토큰 분리 검증, 점검과 독립적인 전달·유지보수 동작은 유지한다.
- 전달 조립 테스트는 실제 `application.yml`을 바인딩하도록 바꾸고, 사용하지 않게 된
  활성 전달 설정 도우미를 제거했다. 별도 불리언 해석기나 런타임 의존성은 추가하지 않았다.
- 2026-08-30 추가 검토에서 확인한 진단 로그 노출과 점검 설정 해석 불일치를 수정했다.
  진단 SQL은 `psql`의 매개변수 바인딩과 트랜잭션 범위 로그 설정으로 리소스 참조를
  보호한다. 서버 전체 로그나 다른 연결의 설정은 바꾸지 않으며, 보호 설정 권한이
  없으면 조회 전에 실패한다.
- 점검 예약 조건은 Spring `Condition`에서 `Environment`의 불리언 변환을 사용한다.
  `on`·`yes`·`1`과 앞뒤 공백을 포함한 값이 설정 바인딩과 같은 결과를 내며,
  사용자 설정 빈을 미리 초기화하거나 직접 불리언 해석기를 추가하지 않았다.
- 2026-08-30 `WATCH_CHECK_ENABLED`를 추가했다. 기본 `true`이며 `false`로 시작한
  인스턴스는 점검 예약만 등록하지 않는다. API·유지보수·활성 이벤트 전달과
  모니터 상태·일정·리스는 유지한다. 실시간 제어 API는 추가하지 않았다.
- 점검과 유지보수 예약 클래스를 분리했다. Prometheus의 `check_enabled: "false"`
  수집 레이블은 점검 미실행·일정 지연 경보만 제외하며 전달·유지보수 경보는 유지한다.
- `ops/staging-monitor-diagnostics.sh`는 기존 PostgreSQL에서 리소스별 현재 상태와
  최근 점검·전달 정보를 읽기 전용으로 조회한다. 각 이력은 기본 50건·최대 100건이고
  SQL 5초·잠금 1초·트랜잭션 10초 상한을 적용한다. URL·호스트·본문·리스 토큰과
  원본 접속 오류는 출력하지 않는다. 운영 DB 조회는 수행하지 않았다.
- [점검 중지·진단 런북](docs/runbooks/check-control-and-diagnostics.md)에 인스턴스별
  중지·재개, Compose 재생성, 경보 레이블과 진단 결과의 해석 범위를 기록했다.
  해당 점검 제어 변경에는 NGINX가 포함되지 않았으며 이번에 별도로 추가했다.
  공개 배포는 여전히 운영 승인이 필요하다.
- 2026-08-30 토큰 검증 테스트는 기존 `BootstrapTestFixtures.watchProperties`를
  재사용하도록 바꾸고 중복 설정 생성 코드를 제거했다.
- 점검·전달 관측값은 각각 HTTP 상태 분류를 한 곳에서 수행한다. 생성자 검증은
  유지하며, HTTP 3xx를 점검 성공·전달 실패로 처리하는 차이도 유지한다.
- MVC와 보안 필터는 `MonitorApiProblem`에서 Spring `ProblemDetail` 생성 규칙을
  공유한다. 미인증 401 응답의 네 필드, 나머지 오류 응답의 고정 `instance`를
  포함한 다섯 필드, 인증 우선순위·헤더·이미 전송된 응답의 처리 방식은 바꾸지 않았다.
- 2026-08-30 기존 Spring 예약 실행 지표를 사용하는 점검·전달·유지보수 미실행
  경보 3개를 추가했다. 시작 후 10분을 유예하고 전달 비활성 환경을 제외한다.
  자체 감시 스레드·추가 DB 조회·운영 메트릭은 만들지 않았다.
- `:adapter-out-persistence:processRecoveryTest`는 별도 JVM을 실제로 강제 종료하고
  점검 30초·전달 60초 리스의 자연 만료 후 복구를 확인한다. 점검 점유 후,
  전달 점유 후, 수신 대역이 요청을 받은 뒤 응답 전 중단의 세 사례가 통과했다.
  운영 부트 JAR·Spring 스케줄러 전체 재시작이나 실제 BATON 연동 시험은 아니다.
- 2026-08-30 라이선스 전역 제외 목록 때문에 미승인 의존성이 검사를 통과하는
  문제를 수정했다. CI와 회귀 테스트는 `ops/check-runtime-licenses.py`를 함께 사용하며,
  허용 라이선스와 승인된 패키지·버전별 예외를 확인한 뒤에만 Trivy 제외 목록을 출력한다.
- 점검·전달 카운터 동시 입력의 경보 평가 오류와 백업 도구 중단 시 하위
  프로세스·임시 환경이 남는 문제의 수정도 유지한다.
- 격리 PostgreSQL에서 동기화·점검·전달 부하와 DB 잠금·콜백 장애 후
  복구를 확인하는 `:adapter-out-persistence:loadTest`를 추가했다. 기본 100개,
  최대 1000개 모니터를 사용하고 일반 테스트와 분리했다. CI는 25개로 실행한다.
- 기존 메트릭을 사용하는 경보 규칙은 11개이며 `promtool` 테스트를 제공한다. 전달
  비활성 환경의 백로그는 경보에서 제외하며 실제 수집기·외부 알림은 연결하지 않았다.
  완료 실패·리스 회수는 점검·전달 증가량을 따로 계산해 합산하며, 한쪽 카운터만
  존재하는 경우와 양쪽 합계만 임계치를 넘는 경우를 모두 처리한다.
- 표준 `pg_dump`로 `0600` 파일을 만들고 별도 임시 PostgreSQL에서 실제 복원하는
  도구를 추가했다. 운영 볼륨·외부 네트워크를 연결하지 않으며 마이그레이션 이력과
  이벤트 백로그 일치를 확인한다. 실제 운영 데이터 백업은 수행하지 않았다.
  종료 신호를 받으면 CLI 하위 프로세스와 임시 파일·복원 환경을 정리한다.
  강제 종료·호스트 중단·Docker 데몬 장애에는 자동 정리를 보장하지 않으며,
  시작 시 출력한 임시 프로젝트 이름으로 수동 정리하는 절차를 런북에 명시했다.
- CI는 DB 백업·복원 통합 테스트, 별도 JVM 복구, 부하·복구 스모크와 경보 규칙을 검사하고 시험
  보고서를 7일 보관하도록 구성했다. 이 변경의 원격 CI 결과는 아직 확인하지 않았다.
- 이전 Java/Spring 정리에서 적용한 `JdbcClient`, Spring 예약 관측 자동 구성,
  Awaitility와 AssertJ 재사용을 유지한다. 당시 운영 기능은 HTTP 응답 계약·DB 스키마와
  런타임 의존성을 바꾸지 않았다. 기존 Jackson을 진단 통합 테스트 의존성으로만 추가했다.
- Java 21 / Spring Boot 4.1.1 기반 모니터링 MVP와 하나의 BATON HTTPS 상태 변경
  콜백 전달이 구현되어 있다.
- 운영 배포, Cloudflare Tunnel 연결, 외부 알림, 프런트엔드, 메시지 브로커는
  구축되거나 검증되지 않았다.
- 관리 포트의 내부 Prometheus 조회, 적용 전 경보 규칙과 대시보드 JSON을 제공한다.
  외부 메트릭 전송, 외부 계정, 상시 대시보드 서버와 알림 수신 경로는 생성하지 않았다.
- BATON 콜백 사전 검사 뒤에만 토큰을 `configtree`로 주입해 전달을 켜는 선택적
  스테이징 이벤트 전달 오버레이가 준비되어 있다. 실제 콜백 전달은 검증하지 않았다.
- Gradle·GitHub Actions·Docker 기본 이미지의 주간 Dependabot 점검을 활성화했다.
  다단계 Dockerfile·Compose·Alpine 패키지·Trivy·Prometheus 검사용 이미지의 갱신 범위는
  `renovate.json`에 준비했지만 외부 Renovate 서비스는 활성화하지 않았다.

## 유지되는 구현 기준

- 제품 범위와 HTTP 계약은 [PRD-0001](docs/PRD/0001_product-baseline/spec.md),
  [PRD-0002](docs/PRD/0002_api-contract/spec.md),
  [PRD-0003](docs/PRD/0003_monitoring-mvp/spec.md),
  [PRD-0004](docs/PRD/0004_health-change-event-delivery/spec.md)를 따른다.
- 저장소·실행·직접 전달 결정은 [ADR-0002](docs/ADR/0002_monitoring-mvp-storage-and-execution/adr.md)와
  [ADR-0003](docs/ADR/0003_health-change-event-delivery/adr.md)를 따른다.
- 외부 설정보다 우선하는 Spring Boot 환경 후처리기가 Tomcat 자원 상한,
  Apache HttpClient와 Spring JDBC 민감 로거 차단, Actuator 허용 목록·상세 비공개,
  65초 예약 작업 종료 대기를 고정한다.
- 스테이징은 같은 Git SHA의 데이터베이스 작업·마이그레이션·WATCH 이미지 세 개를
  사용한다. [배포 런북](docs/runbooks/staging-deployment.md)은 이미지 ID·OCI
  리비전·아카이브 SHA-256 보관과 데이터베이스 소유자·런타임 비밀번호의 별도
  교체·원복 절차를 제공한다.

## 2026-09-02 이전 변경의 검증 상태

- `./gradlew clean test :adapter-out-persistence:loadTest
  :bootstrap:verifyBootJarLicense --no-daemon --no-build-cache`가 통과했다. 일반 테스트
  431개와 기본 100개 모니터 부하·복구 2개가 실패·오류·건너뜀 없이 완료됐다.
- `./gradlew clean test :bootstrap:runtimeLoadTest :bootstrap:verifyBootJarLicense
  --no-daemon --no-build-cache`가 통과했다. 일반 테스트 431개와 25개 모니터 전체
  런타임 시험 1개가 실패·오류·건너뜀 없이 완료됐다. 최종 실행에서 풀 복구 640ms,
  동시 동기화 70ms, 점검 완료 4.989초, 전달 복구 4.490초였고 최종 백로그는 0이었다.
  이 값은 로컬 회귀 참고값이며 지원 규모·처리량·SLO 증거가 아니다.
- [원격 검증 실행](https://github.com/ljkhyeong/baton-watch/actions/runs/33628969130)은
  전체 Gradle, 별도 JVM 복구, 격리 DB 부하, 새 전체 런타임 부하·복구, 운영 도구와
  CodeQL을 통과했다. 마지막 공급망 검사에서 기존과 같이 공식 PostgreSQL·NGINX·
  cloudflared 이미지 세 개가 취약점 기준을 통과하지 못해 전체 작업은 실패했다.
  새 코드나 런타임 시험 실패가 아니며 검사 예외와 보안 기준 완화는 적용하지 않았다.
- 부하·복구 시험은 DB에서 읽은 실제 적체 지연을 기록했고 0ms 대상 사례의 최댓값은
  3초, 20ms 대상 사례는 5초였다. 최종 이벤트 백로그는 두 사례 모두 0이었다.
- `./ops/tests/prometheus-rules-test.sh`가 통과했다. 기존 WATCH 경보 11개와
  인바운드 경보 3개, 유지보수 여섯 작업의 미실행·복구, Grafana 12개 패널의 실제
  PromQL을 검증했다. 실제 수집기·Grafana 서버·외부 알림 연결은 수행하지 않았다.

- 서비스 이름이 다른 응답과 이름이 없는 응답을 추가한 격리 Blackbox 회귀 시험은
  수정 전 두 응답을 모두 `probe_success=1`로 처리해 실패했고 CEL 수정 후 통과했다.
  전체 게이트웨이 시험 2개도 통과하고 시험 컨테이너·네트워크를 정리했다.
- 공용 공급망 검사 회귀 시험은 자체 이미지 아카이브 세 개와 Compose 공식 이미지
  세 개의 정확한 참조를 입력받아 취약점 검사 일곱 번, 라이선스 검사 한 번,
  JAR·SBOM 일곱 개·체크섬 생성과 기존 보고서 비덮어쓰기를 확인했다. 취약점 발견 시
  다른 검사를 끝까지 수행하고 실패 보고서는 남기되 완료 체크섬은 만들지 않는다.
- 실제 ARM64 검사에서 수정 커밋 `c3b21cf`의 부트 JAR과 자체 이미지 세 개는 수정
  가능한 `HIGH`·`CRITICAL` 항목이 0건이었다. Compose 공식 이미지 세 개와 최신
  후보의 결과는 현재 인계 상태에 기록한 건수로 차단됐다. 임시 후보 보고서와 이미지
  아카이브는 확인 후 삭제했다.
- 라이선스 정책 테스트 6개, 공급망 검사 계약, Prometheus 규칙·설정·대시보드 쿼리,
  스테이징 Compose 정책, 새 셸의 ShellCheck와 CI `actionlint`가 통과했다.
- `./gradlew clean test --no-daemon`으로 전 모듈 테스트 431개를 실제 실행해 실패·오류·
  건너뜀 없이 통과했다. 운영 Java·Gradle·DB 스키마가 바뀌지 않아 로컬 부하·별도
  JVM 복구 시험은 반복하지 않았다.
- PR #32의 첫 원격 `Verify` 실행은 전체 Gradle과 별도 JVM 복구 시험을 통과한 뒤,
  `0700` 임시 디렉터리를 비루트 Prometheus 컨테이너에 마운트해 대시보드 쿼리를
  읽지 못하는 Linux 권한 문제를 찾았다. 생성 파일 하나만 `0444`로 읽기 전용
  마운트하도록 수정했으며 `umask 077` 로컬 실제 Prometheus 검사가 통과했다.
- 두 번째 원격 `Verify` 실행은 Prometheus를 포함해 공급망 검사 전 모든 단계와
  CodeQL을 통과했다. 공식 이미지 세 개를 모두 차단하고 JAR 라이선스 검사까지
  마친 뒤, root로 실행한 Trivy가 만든 보고서·캐시를 호스트 사용자가 정리하지 못하는
  문제가 드러났다. Trivy를 호스트 UID/GID로 실행하고 캐시를 전용 경로에 두도록
  수정했다. 로컬 실제 전체 검사에서 일곱 보고서가 모두 `0600`으로 남고 자체 산출물
  4개는 0건, 공식 이미지 세 개는 23건·1건·12건으로 차단되며 정리 오류가 없음을
  확인했다.
- 세 번째 원격 `Verify` 실행은 앞선 모든 검증과 CodeQL을 다시 통과하고 공급망
  검사에서만 실패했다. PostgreSQL·NGINX·cloudflared를 모두 차단한 뒤 권한·정리
  오류 없이 실패 보고서 보관도 성공했다. 원격 산출물을 내려받아 부트 JAR과 자체
  이미지 세 개는 0건, 공식 이미지 세 개는 23건·1건·12건이며 SBOM 일곱 개가 모두
  존재하고 완료 체크섬은 없음을 확인했다. 실제 배포·외부 연결은 실행하지 않았다.

### 직전 인바운드 주 경보 수정의 검증 기록

- 새 교차 실패 사례는 수정 전 주 경보가 발생하지 않아 실패했고 수정 후 통과했다.
  1분 단위 평가에서 2분째 경보가 발생하고 10분째까지 모든 평가에 유지됐다.
  11분째 정상 응답에서 해제되며 12분째의 단발성 오류에는 다시 발생하지 않는다.
- 수집 실패 중 이전 성공 지표가 남아 있는 경우와 성공 지표가 없는 경우에 주 경보와
  수집 실패 보조 경보가 함께 발생하는지 확인했다. 일부·전체 수집 대상 누락도 유지한다.
- `./ops/tests/prometheus-rules-test.sh`가 통과했다. 기존 경보 11개와 인바운드 경보
  3개의 문법, 수집 설정, 인바운드 5개 시나리오와 기존 작업자·대시보드 쿼리를 검사했다.
- 수정한 문서 4개의 로컬 링크 46개와 `git diff --check`가 통과했다.
- 운영 Java·Gradle·Compose·이미지가 바뀌지 않아 일반 JUnit 431개, NGINX 격리 시험과
  취약점 검사를 반복하지 않았다. 원격 CI·실제 배포·외부 연결도 실행하지 않았다.

### 직전 NGINX 공급망·인바운드 점검의 검증 기록

- 실제 NGINX·Blackbox Exporter·HTTP 대역을 사용한 격리 테스트 2개가 통과했다.
  정상 응답, 공개 모듈의 평문 거부, 리다이렉트 비추적, 잘못된 JSON 본문과
  백엔드 중단·복구를 확인했다. 백엔드 중단 중에도 프록시 생존 확인은 성공하지만
  `probe_success`는 0이다. 운영 WATCH·공개 HTTPS·Cloudflare·알림 수신 검증은 아니다.
- 첫 실행에서는 Blackbox의 리다이렉트 수를 따라간 횟수로 잘못 해석한 테스트가
  실패했다. 최종 HTTP 상태가 302로 유지되는지 확인하도록 바로잡은 뒤 통과했다.
- 기존 경보 11개, 선택적 인바운드 경보 3개, Prometheus 수집 설정 문법과 기존
  대시보드 PromQL 검사를 통과했다. 새 경보 사례는 요청 실패·복구, Exporter 수집
  실패, 성공 지표 누락, 일부·전체 수집 대상 누락을 확인한다.
- 실제 Trivy 검사는 SBOM 구성요소 22개를 생성하고 위 OpenSSL 취약점 1개
  (영향 패키지 2개)로 차단됐다. 취약점 검사가 통과한 것은 아니다.
- Blackbox의 `--config.check`, Prometheus의 서비스 발견 결과에서 고정 레이블·
  두 모듈·주소 치환, Compose 정책, ShellCheck, actionlint와 문서 로컬 링크·
  `git diff --check`가 통과했다. 시험 컨테이너와 네트워크는 정리했다.
- 운영 Java·Gradle·DB 스키마가 바뀌지 않아 이전의 일반 JUnit 431개와 부트 JAR
  라이선스 검사를 반복하지 않았다. 원격 CI·실제 배포·외부 연결도 실행하지 않았다.

### 직전 요청 제한·준비 상태·대시보드의 검증 기록

- 실제 NGINX와 HTTP 대역을 쓰는 격리 검사에서 상태·모니터 각각의 429, 독립적인
  요청 예산, Bearer 헤더 전달, 큰 본문 요청의 인증 우선 전달, 관리 경로 차단과
  민감 값 없는 로그를 확인했다. WATCH 인증 자체와 공개 Cloudflare 연동 시험은 아니다.
  시험용 컨테이너·네트워크는 정리했다.
- 실제 Spring 전체 조립에서 정상·DB DOWN 대역·요청 수락 거부·복구를 확인했다.
  readiness만 503으로 바뀌며 liveness는 UP을 유지하고 상세 DB 정보는 노출하지 않는다.
  DB 네트워크 장애나 실제 프로세스 종료 주입 시험은 아니다.
- 최초 전체 실행에서 DB를 제외한 보안 전용 테스트의 readiness 구성 충돌을 확인했다.
  해당 테스트만 `readinessState`로 맞춘 뒤 보안 사례 13개와 전체 회귀를 다시 통과했다.
  운영 DB 필수 조건이나 Spring 상태 그룹 검증을 끄지 않았다.
- `./gradlew test :bootstrap:verifyBootJarLicense --no-daemon --no-build-cache
  --rerun-tasks --max-workers=2 --no-parallel`이 통과했다. 60개 스위트·431개
  일반 JUnit 테스트에서 실패·오류·건너뜀은 0이다. 부트 JAR Apache-2.0도 확인했다.
- 로컬 Compose와 스테이징 기본·터널·이벤트 전달 오버레이의 렌더링·정책 검사가 통과했다.
  `promtool`은 기존 경보 11개와 대시보드 실제 쿼리의 두 인스턴스·수집 누락 입력을 통과했다.
- 임시 Grafana 13.1.0에서 JSON 가져오기가 성공했다. 실제 Prometheus 데이터 소스는
  연결하지 않았고 확인 후 컨테이너를 제거했다. 화면 렌더링이나 외부 수집 검증은 아니다.
- 변경된 셸의 ShellCheck, CI `actionlint`, 문서 로컬 링크와 `git diff --check`를 통과했다.
- 운영 DB·BATON 콜백·Cloudflare를 호출하거나 변경하지 않았다. 실제 배포, 유료 서비스,
  외부 알림, 새 서버, 원격 CI 실행·PR 생성은 수행하지 않았다. 배포 이미지 세 개의
  재빌드·취약점 검사와 별도 부하·JVM 복구 시험은 이번에 반복하지 않았다.

### 직전 배치 인터럽트 처리의 검증 기록

- 새 회귀 테스트는 점검·전달 각각 시작 전 인터럽트와 첫 외부 호출 중 인터럽트를
  확인한다. 수정 전에는 네 사례 모두 불필요한 점유 호출로 실패했으며 수정 후 통과했다.
  기존 테스트 대역과 고정 `Clock`을 재사용했고 테스트가 남긴 인터럽트는 정리한다.
- 좁은 범위의 점검·전달 배치 테스트 14개가 통과했다. 이전 검토의 두 항목 재현에서도
  시작 전 인터럽트는 점유·완료 0건, 첫 호출 중 인터럽트는 점유·완료 1건으로 바뀌었다.
  인터럽트 이후 새 점유는 모두 0건이고 인터럽트 상태는 유지됐다. 저장소 대역을 사용한
  서비스 검증이며 운영 DB나 전체 프로세스 종료를 검증한 결과는 아니다.
- `./gradlew test :bootstrap:verifyBootJarLicense --no-daemon --no-build-cache
  --rerun-tasks --max-workers=2 --no-parallel`이 통과했다. 모든 모듈을 다시
  컴파일·실행했으며 일반 JUnit 60개 스위트·430개 테스트에서 실패·오류·건너뜀은 없다.
  부트 JAR의 Apache-2.0 라이선스도 확인했다. 의존성·스키마 변경은 없다.
- 운영 DB와 실제 BATON 콜백을 조회·변경하거나 호출하지 않았다.
- `git diff --check`를 통과했다. Compose·경보 규칙·셸 코드는 변경하지 않았으며
  별도 Compose 정책·`promtool`·ShellCheck·`actionlint` 검사와 부하·별도 JVM 복구
  시험은 반복하지 않았다.
- 원격 CI, 배포 이미지 빌드, 실제 공개 스테이징 배포, 외부 HTTPS 스모크와
  외부 로그 감사는 이번 작업에서 수행하거나 성공을 확인하지 않았다.
- PR은 최신 HEAD에서 `Verify / verify`가 성공해야 병합할 수 있다.

### 직전 DNS 대기 조회 취소 수정의 검증 기록

`7df650b`까지의 작업에서는 DNS 테스트 7개가 통과했다. 큐 대기 조회를 종료하면
리졸버를 호출하지 않고 10초 조회 기한 대신 1초 내에 `INTERNAL_FAILURE`로 반환했다.
당시 전체 일반 JUnit 426개와 부트 JAR 라이선스 검사에서 실패·오류·건너뜀은 없었다.
실행 중인 JVM 리졸버의 강제 중단을 검증하거나 보장한 것은 아니다.

### 직전 HTTP 요청 취소 수정의 검증 기록

`f0c0841`까지의 작업에서는 좁은 범위의 HTTP 실행기·GET·POST·TLS 테스트 22개가
통과했다. 실제 로컬 스트리밍 응답 중 GET의 시간 초과·호출자 중단·실행기 종료가
연결을 닫고, GET·POST의 시간 초과 뒤 같은 단일 작업자가 정상 요청을 처리했다.
종료 시 대기 요청은 실행되지 않고 호출자가 반환했다. Host·SNI·TLS 검증,
Bearer·멱등성 헤더, 리다이렉트 금지와 응답 크기 제한도 확인했다. 당시 전체
일반 JUnit 425개와 부트 JAR 라이선스 검사를 통과했으며 공개 HTTPS 콜백 검증은 아니다.

### 직전 이벤트 전달 활성화 조건 수정의 검증 기록

`f130b43`까지의 작업에서는 실제 설정 바인딩을 사용하는 전달 조립 테스트 14개와
예약 테스트 14개가 통과했다. 점검을 끈 상태에서 전달을 켜면 예약 6개, 함께 끄면
유지보수 예약 5개가 등록된다. 당시 전체 일반 JUnit 420개와 부트 JAR 라이선스
검사를 통과했고 설정 도우미 정리 후 bootstrap 87개도 다시 실행했다.

### 직전 진단 로그·점검 설정 수정의 검증 기록

`1d6b857`까지의 작업에서는 일반 JUnit 407개와 부트 JAR 라이선스 검사가 통과했다.
별도 네트워크 없는 임시 PostgreSQL에서 로그 보호 설정 권한이 없으면 실제 CLI가
조회 전에 실패하고 리소스 참조가 서버 로그에 남지 않음을 확인한 뒤 환경을 정리했다.
이 권한 부족 수동 재현은 이번 전달 설정 수정에서 반복하지 않았다.

### 직전 점검 제어·진단 도구 추가의 검증 기록

`0d4589f`까지의 작업에서는 전체 새 빌드의 일반 JUnit 397개와 부트 JAR 라이선스
검사가 통과했고 의존성 체크섬 변경은 없었다. 이후 진단 테스트 8개를 다시 실행했다.
당시 `promtool` 규칙 11개·시나리오 그룹 9개, 스테이징 Compose 정책, 로컬·스테이징의
점검 비활성 렌더링, Bash 구문·ShellCheck·`actionlint`가 통과했다. 이 기록은
이번 수정에서 해당 운영 검사들을 재실행한 결과가 아니다.

### 직전 리팩터링의 검증 기록

`e7b3db7`까지의 리팩터링에서는 HTTP 상태 경계, 토큰·MVC·보안 오류 계약을
검증했다. 당시 일반 JUnit 386개와 부트 JAR 라이선스 검사가 통과했고
실패·오류·건너뜀은 없었다. 이 수치는 이번 운영 기능 추가 이전의 결과이며,
아래 경보·복구 작업의 검증 기록과도 별도로 구분한다.

### 직전 경보·복구 시험 작업의 검증 기록

다음은 `4bb4cf1`까지의 작업에서 확인한 결과이며 이번 리팩터링에서는 반복하지 않았다.

- `./ops/tests/prometheus-rules-test.sh`에서 규칙 11개와 두 테스트 파일의
  8개 시나리오 그룹이 통과했다. 검사는 시작 유예·타이머 누락·유휴 배치·
  카운터 초기화·다중 인스턴스·유지보수 일부 중단·복구를 포함한다.
- 실제 전체 조립 스모크에서 Actuator의 `process_uptime_seconds`와 빈 배치를
  포함한 예약 메서드 7개의 성공 완료 지표를 확인했다.
- `:adapter-out-persistence:processRecoveryTest` 3개가 실패·오류·건너뜀 없이
  통과했다. 사례별 시간은 점검 복구 약 33초, 전달 요청 전 복구 약 63초,
  수신 후 응답 유실 복구 약 62초다. 시각을 조작하지 않았으며 종료 후 시험용
  자식 JVM이 남지 않은 것도 확인했다.
- `./gradlew test :adapter-out-persistence:loadTest :bootstrap:verifyBootJarLicense
  --no-daemon --no-build-cache`가 통과했다. 당시 일반 JUnit 보고서는 370개 모두
  성공이었다. 변경된 영속성·bootstrap 테스트를 재실행하고 변경 없는 모듈은
  Gradle의 최신 결과를 재사용했다. 기본 100개 모니터의 부하·복구 2개도 실행해 최종
  백로그 0을 확인했다.
- CI의 필수 증거 검사 코드를 로컬 보고서에 적용해 스위트 10개·테스트 48개의
  실행·실패 없음·건너뜀 없음을 확인했다. 원격 CI 성공을 확인한 기록은 아니다.
- CI 구문 `actionlint`, 경보 셸의 Bash 구문과 `git diff --check`를 통과했다.
- Docker 재기동 중 첫 경보·조립 검사는 연결 실패했으나 연결 복구 후 재실행해
  통과했다. 검사를 건너뛰어 성공 처리하지 않았다.
- 라이선스 수정 `fe3809e`의 정책 회귀 6개와 실제 SBOM 구성요소 76개 검증
  결과는 유지한다. 해당 검사와 취약점 스캔·배포 이미지 빌드는 이번에 반복하지 않았다.

### 이전 작업에서 인계된 운영 검증 기록

다음 운영 검증은 이전 작업에서 남긴 결과이며 이번 변경에서 재실행하지 않았다.
당시 이미지 빌드의 정확한 코드 리비전은 이 문서에 기록되어 있지 않다.

- 실제 PostgreSQL 18.6 격리 검증에서 런타임 역할 속성·검색 경로·역할 소속·객체
  소유 금지와 소유자가 새로 만든 테이블·시퀀스·함수의 기본 권한 차단을 확인했다.
  런타임·소유자 비밀번호를 각각 교체한 뒤 이전 자격 증명이 거부됐고, 원복 뒤
  교체된 자격 증명이 거부됐으며 WATCH 재기동이 정상 완료됐다.
- 기본·터널·선택적 이벤트 전달 오버레이의 스테이징 Compose 정책, 이미지 ID·
  아카이브 체크섬·복원, 데이터베이스 비밀 비노출, 이벤트 전달 사전 검사와 Compose 렌더링
  검증이 통과했다.
- 비파괴 운영 셸 테스트 7종을 재실행했다. 공개 스모크 16개, 이벤트 전달 사전
  검사 9개, URL 원문 경계 27개, 로그 비식별화 감사 7개 격리 사례를 검증했다.
  공통 URL 정책의 Python 구문, 전체 운영 셸 파일의 Bash 구문과 로컬 고정
  `koalaman/shellcheck:v0.11.0` 정적 검사가 통과했다.
- 당시 코드로 배포 이미지 세 개를 빌드한 뒤 실제 PostgreSQL 역할 분리,
  Flyway V1~V4, 비밀번호 교체·원복과 WATCH 재기동 스모크가 통과했다. 이미지는
  외부 레지스트리에 게시하거나 배포하지 않았다.

## 공개 배포 차단 조건

다음 항목이 모두 승인되고 증거가 남기 전에는 공개 배포하지 않는다.
저장소의 시험·경보·백업 도구만으로 아래 운영 조건이 해소되지는 않는다.

- 공개 목적지만 허용하는 인프라 DNS·HTTP/HTTPS 이그레스 정책
- 활성 모니터·동기화·이벤트 폭주 부하 시험과 지원 규모·SLO
- NGINX 상태·모니터 경로별 초기 제한값의 부하 승인과 `429` 외부 검증. 비용이
  발생할 수 있는 Cloudflare 유료 요청 속도 제한은 현재 범위에서 제외한다.
- 점유·완료 실패, 리스 회수, 일정 지연, 전달 백로그, 데이터베이스 시계 편차를
  포함한 외부 대시보드·알림 경로·임계치와 장애 주입 증거
- Cloudflare 계정·터널 토큰·DNS/TLS와 호환 BATON 콜백의 실제 준비
- 비어 있지 않은 공개 스테이징을 위한 별도 암호화 백업 위치, 승인된 보존 기간,
  암호화 키·복사·만료 삭제·정기 복원 시험 담당

## 다음 작업 진입점

1. [초안 PR #32](https://github.com/ljkhyeong/baton-watch/pull/32)는 공식 이미지
   취약점 때문에 `Verify / verify`가 공급망 검사에서 차단된 상태다. 수정된 공식
   이미지 다이제스트로 전체 검사를 통과한 뒤 PR을 준비 상태로 바꾸고 병합하며,
   `main`의 공급망 출처 증명 결과까지 확인한다.
2. [부하·복구 시험](docs/runbooks/load-recovery-test.md),
   [전체 Spring 런타임 부하·복구 시험](docs/runbooks/runtime-load-test.md),
   [별도 JVM 복구 시험](docs/runbooks/process-recovery-test.md),
   [경보·대시보드 적용 조건](docs/runbooks/monitoring-alerts.md),
   [NGINX 요청 제한](docs/runbooks/request-rate-limit.md), 배포 런북의 백업 절차를
   운영 환경 검증의 시작점으로 사용한다.
3. 공개 배포를 추진한다면 위 차단 조건부터 별도 운영 승인으로 해소한다.
4. 승인 뒤에만 [스테이징 배포 런북](docs/runbooks/staging-deployment.md)을 처음부터
   실행하고 내부·외부 스모크와 로그 비식별화 증거를 보관한다.
5. BATON 전달 검증은 스테이징 배포가 끝난 뒤
   [공개 전달 런북](docs/runbooks/public-staging-event-delivery.md)으로 별도 수행한다.
